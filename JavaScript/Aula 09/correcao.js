const calcularIMC = () => {
    let altura = Number(document.querySelector(#'altura').value)
    let peso = Number(document.querySelector(#'peso').value)
    let pagrafo = document.querySelector('p')
    let imc = peso / (altura*altura)
    paragrafo.innerHTML = 'Seu IMC é ${}'
}